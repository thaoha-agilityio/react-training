import { colors } from "./colors";
import { fontSizes, fontWeights, fontFamilies, lineHeights } from "./font";
import { widths, heights, margins, paddings } from "./metrics";

export const themes = {
  colors,
  fontSizes,
  fontWeights,
  fontFamilies,
  lineHeights,
  widths,
  heights,
  margins,
  paddings,
};
